apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-dataextraction
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: {{ .Release.Name }}-dataextraction
  ports:
    - protocol: TCP
      port: 8000
